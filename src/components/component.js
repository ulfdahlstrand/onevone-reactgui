/** @jsx React.DOM */

var React = require('react');

var Component = React.createClass({
  render: function () {

    return <div>Hello world! it works live too qwerty</div>;
  }
});

module.exports = Component;
